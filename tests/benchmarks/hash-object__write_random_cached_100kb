#!/bin/bash -e

. "$(dirname "$0")/benchmark_helpers.sh"

gitbench --prepare "sandbox_repo empty_standard_repo &&
                    create_random_file random_100kb 102400" \
         --warmup 5 \
         --chdir "empty_standard_repo" \
         hash-object "../random_100kb"
